import styles from './styles/tab.module.css'
const Tab = ({items}) => {
    return(
        <div className={`${styles.tab}`}>
            <div className={`${styles.tab__titles} flex`}>
                
            </div>
            <div className={`${styles.tab__contents}`}>

            </div>
        </div>
    )
}

export default Tab